# Leave unset in dev — Vite proxies /api to localhost:8000 (see vite.config.ts).
# Set this in production if the frontend and backend are on different Render services.
# VITE_API_BASE_URL=https://solarix-backend.onrender.com
